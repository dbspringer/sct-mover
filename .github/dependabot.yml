version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # One PR for all the action bumps, rather than one per action.
    groups:
      github-actions:
        patterns:
          - "*"
